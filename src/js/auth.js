let auth = {}

/**
 * Check if user is logged
 * @returns boolean
 */
auth.isLogged = async() => {
    return false
}

/**
 * Logout user
 * @returns boolean
 */
auth.logout = async() => {
    return true
    
}

export default auth; 